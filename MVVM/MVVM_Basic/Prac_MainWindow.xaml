<Window x:Class="Mvvm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mvvm"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="ViewModel">
    
    <StackPanel Orientation="Vertical">
        <TextBox x:Name="tb1" Text="{Binding Model.Num}" Width="200"/>
        <TextBox x:Name="tb2" Text="{Binding Model.Name}" Width="200"/>
        <TextBlock x:Name="tbl" Text="{Binding Model.Res}"/>
        <Button Content="문자연결" Command="{Binding Cmd}"/>
        <Button Content="저장" Command="{Binding SaveCmd}"/>
        <!--SelectedIndex는 선택한 항목의 위치 값, SeletedItem은 선택한 항목의 value-->
        <ListBox ItemsSource="{Binding Plist}" SelectedIndex="{Binding idx}" SelectedItem="{Binding P}">
            <!--이벤트 트리거-->
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="SelectionChanged">
                    <b:InvokeCommandAction Command="{Binding CmdSelChanged}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Num}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Window>
