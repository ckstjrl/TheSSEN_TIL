<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="ViewModel.ProdViewModel">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock TextWrapping="Wrap" Text="{Binding PModel.Id}" Height="43" FontSize="20"/>
            <TextBox TextWrapping="Wrap" FontSize="20"  Text="{Binding PModel.Name}"/>
            <TextBox TextWrapping="Wrap"  Width="398" FontSize="20" Text="{Binding PModel.Price}"/>
            <TextBox TextWrapping="Wrap"  FontSize="20" Text="{Binding PModel.Amount}"/>
            <Button Content="추가" Command="{Binding CmdSave}"/>
            <Button Content="수정" Command="{Binding CmdEdit}" />
            <Button Content="삭제" Command="{Binding CmdDel}" />

        </StackPanel>
        <StackPanel Grid.Column="1">
            <TextBlock TextWrapping="Wrap" Text="상품목록" FontSize="20"/>
            <ListBox ItemsSource="{Binding Plist}" SelectedIndex="{Binding SelectedIdx}" Height="299" Width="355" >
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="SelectionChanged">
                        <b:InvokeCommandAction Command="{Binding CmdSel}" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Id}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </StackPanel>


    </Grid>
</Window>
